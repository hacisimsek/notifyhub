server:
  port: 8082

spring:
  application:
    name: reminder-service

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
